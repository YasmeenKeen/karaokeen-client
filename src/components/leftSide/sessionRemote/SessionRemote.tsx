import React from "react";

type Props = {};

const SessionRemote = (props: Props) => {
  return (
    <div className="ToDo">
      SessionRemote
      <p style={{width:'70%'}}> This panel will include a remote feature in the future where over users can scan a QR code and join the session to add music to the queue</p>
    </div>
  );
};

export default SessionRemote;
